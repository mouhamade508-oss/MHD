[phases.setup]
nixPkgs = ["php", "phpExtensions.pdo_mysql", "phpExtensions.mbstring", "phpExtensions.bcmath"]

[phases.build]
cmds = ["composer install --no-dev"]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"